---
title: "Delete links"
api: "POST https://api.usefini.com/api/v1/public/bots/links/delete"
description: "This endpoint allows you to delete links from your existing bot"
---

### Headers

<ParamField header="token" type="string" required>
  This is the unique api-key for your bot 
</ParamField>

### Body

<ParamField body="bot_links" type="string" required>
List of links to be deleted from your bot. Example [sample_bot_link_1, sample_bot_link_2]
</ParamField>

### Response

<ResponseField name="message" type="string">
  Indicates whether the URLs were successfully updated.
</ResponseField>


<RequestExample>

```python Example Request- Python
import requests

url = 'https://api.usefini.com/api/v1/public/bots/links/delete'
token = "your_api_key_here"
headers = {
    'authorization': 'Bearer token',
}

data = {
    "bot_links": [ bot_link_1, bot_link_2]
}

response = requests.post(url, json=data, headers=headers)
print(response.json())
```

```javascript Example Request- Node.js
const axios = require('axios');

const url = 'https://api.usefini.com/api/v1/public/bots/links/delete';
const token = 'your_api_key_here'; // Replace with your actual API key

const headers = {
  'authorization': `Bearer ${token}`
};

const data = {
  "bot_links": ["bot_link_1", "bot_link_2"]
};

axios.post(url, data, { headers })
  .then(response => {
    console.log(response.data);
  })
  .catch(error => {
    console.error('Error:', error);
  });
```

</RequestExample>

<ResponseExample>

```json Response
{
    "message": "Bot urls updated",
}
```

</ResponseExample>
